import { ArrowBgColorDirective } from './arrow-bg-color.directive';

describe('ArrowBgColorDirective', () => {
  it('should create an instance', () => {
    const directive = new ArrowBgColorDirective();
    expect(directive).toBeTruthy();
  });
});
